
<% if params[:customer_ids].present? && flash[:success] %>
  <% params[:customer_ids].each do |id| %>
    <%= turbo_stream.remove "customer_#{id}" %>
  <% end %>
  <%= turbo_stream.replace "pagy_nav_container" %>
  <%= turbo_stream.replace "count", render(TitleCountComponent.new(title: 'zones', count: Customer.count)) %>
<% end %>

<%= turbo_stream.prepend "notification", partial: "layouts/shared/notification" %>