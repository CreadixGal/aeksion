<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing <%= @customer.name %> </h1>
  <%= turbo_frame_tag dom_id(@customer) do %>
    <%= render 'form', customer: @customer %>
  <% end %>
</div>
