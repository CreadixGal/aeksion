<%= form_with url: search_movements_path, method: :post, autocomplete: 'disabled' , class:"w-full flex justify-end", data: { controller: 'debounce', "debounce-target": 'form', 'searcher-target': 'input' }  do |form| %>
  <%= form.search_field :name, value: [params[:name], params[:kind]], placeholder: 'search customer', data: { action: "input->debounce#search" }, class:'w-full my-3', autocomplete: 'off' %>
<% end %>

