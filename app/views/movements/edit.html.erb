<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Edit</h1>
  <%= turbo_frame_tag dom_id(@movement), class: 'w-full flex justify-between' do %>
    <%= render 'edit_form', movement: @movement %>
  <% end %>
</div>
