<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing <%= @zone.name %> </h1>
  <%= turbo_frame_tag dom_id(@zone) do %>
    <%= render 'form', zone: @zone %>
  <% end %>
</div>
