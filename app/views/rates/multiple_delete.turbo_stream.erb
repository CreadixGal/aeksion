
<% if params[:rate_ids].present? %>
  <% params[:rate_ids].each do |id| %>
    <%= turbo_stream.remove "rate_#{id}" %>
  <% end %>
<% end %>

<%= turbo_stream.prepend "notification", partial: "layouts/shared/notification" %>