<div class="w-full min-h-screen">
  <div class="flex flex-col items">
    <%= render 'pages/shared/tabs' %>
  </div>

  <div class="w-full flex flex-col gap-8 mt-8 px-10">
    <%= turbo_frame_tag :tab_content do %>
      <h1 class="w-full font-bold text-4xl text-center">Pages#index</h1>
    <% end %>
    <%= turbo_frame_tag :dashboard, src: dashboard_path, loading: :lazy do %>
      <div class="w-12 h-12 border-2 border-red-600 rounded-full loader"></div>
    <% end %>
  </div>
</div>
