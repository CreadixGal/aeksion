<!DOCTYPE html>
<html>
  <head>
    <title>Aeksion</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="fixed inset-0 px-4 py-6 pointer-events-none sm:p-6 sm:items-start sm:justify-end">
      <div id="notifications" class="flex flex-col items-end justify-center">
        <%= render "layouts/shared/notification" %>
      </div>
    </div>
    <main class="flex justify-between transition-all duration-300">
      <%= render 'layouts/shared/aside_menu' %>
      <div class="w-[95%] flex flex-col">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
